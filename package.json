{
  "name": "purescript-play",
  "version": "0.2.0",
  "description": "Play UI Layout Package for PureScript",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "spago build",
    "bundle-constructor": "spago bundle --module Demo.Constructor --outfile ./web/constructor.js --platform browser --bundle-type app",
    "bundle-demo": "spago bundle --module Test.Demo --outfile ./web/app.js --platform browser --bundle-type app",
    "bundle-kanji": "spago bundle --module Demo.OnlyKanji --outfile ./web/kanji.js --platform browser --bundle-type app",
    "test": "spago text"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shamansir/purescript-play.git"
  },
  "author": "shamansir",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.15.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "parcel": "^2.15.4"
  }

}
